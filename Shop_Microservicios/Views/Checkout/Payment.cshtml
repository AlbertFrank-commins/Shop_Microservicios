@model Shop_Microservicios.Models.Api.Orders.OrderResponse
@{
    ViewData["Title"] = "Checkout - Pago";
}

<div class="container mt-4">
    <div class="row g-3">

        <div class="col-lg-5">
            <div class="app-card p-4">
                <h4 class="mb-3">Resumen</h4>

                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted">Orden</span>
                    <strong>#@Model.Id</strong>
                </div>

                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted">Total</span>
                    <strong style="font-size:1.15rem;">@Model.TotalAmount.ToString("C")</strong>
                </div>

                <hr />
                <small class="text-muted">
                    Demo: si el último dígito de la tarjeta es par se aprueba; si es impar se rechaza.
                </small>

                <div asp-validation-summary="ModelOnly" class="text-danger mt-3"></div>
            </div>
        </div>

        <div class="col-lg-7">
            <div class="app-card p-4">
                <h4 class="mb-3">💳 Pago con tarjeta</h4>

                <form asp-action="Pay" method="post" novalidate>
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="orderId" value="@Model.Id" />

                    <div class="mb-3">
                        <label class="form-label">Nombre del titular</label>
                        <input class="form-control" name="cardHolder" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Número de tarjeta</label>
                        <input class="form-control" name="cardNumber" placeholder="4111 1111 1111 1112" required />
                    </div>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Vencimiento (MM/AA)</label>
                            <input class="form-control" name="expiry" placeholder="12/29" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">CVV</label>
                            <input class="form-control" name="cvv" placeholder="123" required />
                        </div>
                    </div>

                    <button class="btn btn-primary btn-lg w-100 mt-4">Pagar ahora</button>
                </form>

                <a asp-controller="Orders" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-link mt-2">
                    ← Volver a la orden
                </a>
            </div>
        </div>

    </div>
</div>
